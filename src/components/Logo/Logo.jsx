import css from "./Logo.module.css";

export const Logo = () => {
  return (
    <div className={css.logo2}>
      <button>
        <h2>Questify</h2>
      </button>
    </div>
  );
};

export default Logo;
